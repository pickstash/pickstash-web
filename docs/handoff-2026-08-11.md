# 인수인계 — 소셜 개편 + 홈 리디자인 (2026-08-11)

다른 노트북에서 이어서 작업하기 위한 현재 상태 스냅샷.

## 0. 기본
- **브랜치**: `feature/toss-integration` (main 자동배포 대상 아님 — 웹 운영 안전)
- **최신 커밋**: `58724c8` (그 위로 계속 진행)
- **받기**: 다른 노트북에서 `git fetch && git checkout feature/toss-integration && git pull`
- **env(중요)**: `toss/.env`는 gitignore라 **git으로 안 옴**. 다른 노트북에 없으면 `.ait` 빌드 시 광고 배너·noti 템플릿이 빠짐. `pickstash-handoff/`(USB/에어드랍)에 `toss/.env`·`.env.local` 있음. → 그거 복사할 것.
- **빌드**: `.ait` = `cd toss && npm run build` → deploymentId 나옴 → 콘솔 업로드. 웹 dev = `npm run dev`(https, 8888은 토스 dev).

## 1. 지금까지 완료 (커밋됨)
### 화면 개편 (M1)
- 홈: 체크형 mode-aware(진행률), 단일 스트림, 서랍 그룹카드, 히어로 마감임박만, FAB, 홈/상자 차별화.
- 탭바 **5탭**: 홈 / 상자 / 돋보기 / 알림 / 프로필. 상자 탭 = 전체 스트림(활동순)+종류필터.

### 소셜 백엔드 (M2) — **마이그레이션 036~042 전부 라이브 DB 적용·검증 완료**
- 036 @handle+bio · 037 boxes.visibility+tags · 038 follows · 039 bookmarks · 040 공개열람 RPC(익명화) · 041 참여신청/승인 · 042 내 프로필/저장함 RPC. 전부 추가전용·idempotent.

### 소셜 프론트 (M3) — 완료
- API `src/lib/api/social.ts`, 돋보기(`/explore`), 공개뷰어(`/p/[id]`, 익명화), 남 프로필(`/u/[handle]`), 내 프로필 인스타식(`/profile` + 저장함, 설정은 `/profile/settings`), 팔로우/북마크, 공개 토글+태그(상자 상세 ⋯), @handle 설정(프로필 관리), 참여 신청 승인 UI, 알림 문구.

## 2. 지금 진행 중 (WIP) — 홈 리디자인 탐색
- **`src/app/mockup/page.tsx`** = 버리는 목업. 컨셉 "다정한 손그림 창고: 창고→서랍→상자".
- **보는 법**: 토스 `/boxes` 라우트를 이 목업으로 **임시 스왑**해둠(`toss/src/App.tsx`) → 앱 **상자 탭** 누르면 목업. 상단 "서랍 있는 유저" 체크 + "스택/스와이프" 토글.
- 탐색 중인 것:
  - 서랍 = **드로어 카드**(탭·색으로 상자와 구분), **겹쳐 쌓임(스택)** 또는 **가로 스와이프**로 많아도 대응, 열면 안의 상자.
  - 상자 카드 v2 = 좌측 종류 색 스트립(결정=버터/체크=잎), 진행바·결과 스탬프.
  - **서랍은 선택적 그룹**(필수 아님) — 낱개 상자도 1급, "정리 대기"식 압박 X.
  - 서랍 없는 유저 = 상자 + 가벼운 서랍 안내.

## 3. 다음 할 일 (우선순위)
1. **홈 리디자인 방향 확정**: 서랍 인터랙션 스택 vs 스와이프(또는 혼합), 겹침 정도·색·열기 방식. → 확정되면 목업을 **실제 컴포넌트에 반영**: `home-view.tsx`, 서랍(`folder-group-cards.tsx` 대체), 상자 카드(`home-stream-card.tsx`/`box-card.tsx`).
2. **상자 탭 최종 결정** — "나열된 느낌" 우려로 아직 고민(검색/찾기용으로 특화 vs 유지 vs 제거). 홈이 서랍+상자를 품으면 역할 재정의 필요.
3. **목업 정리(원복)**: `toss/src/App.tsx`의 `/boxes` → `<BoxesScreen/>` 복구 + `BoxesScreen` import 주석 해제, `src/proxy.ts`의 `/mockup` 제거, `src/app/mockup/` 삭제.
4. (선택) 팔로우 알림(user-level) — 현재 참여신청/승인만 box_activities로. 팔로우 알림은 미구현(후속).
5. `.ait` 실기기 e2e: 공개→탐색→신청→수락, 프로필 공개/저장함, @handle.

## 4. 확정된 설계 (기준 문서)
- **`docs/redesign-2026-08.md`** — 개편 확정안 8결정(홈=작업공간, 공개 옵트인, 팔로우 단방향, @handle, 태그, 북마크, 참여 승인, 익명화 등).
- **`docs/redesign-2026-08-impl.md`** — 실행(구현) 계획 M0~M4.
- **`docs/spec.md`** — 전체 기능 명세(단일 기준).

## 5. 주의/게차(gotcha)
- **토스 화면 우상단 비우기** — 토스 시스템 버튼(···/X) 자리. 헤더 우측에 액션 두지 말 것.
- **마이그레이션**: 대시보드 수동·idempotent·추가전용(무중단). 코드 push 전에 적용. 036~042는 이미 적용됨.
- **커밋 author**: 개인 계정(`youngae.kim <kya754@gmail.com>`) 확인. 글로벌(회사) config 건드리지 말 것.
- `[id]`/`[handle]` 대괄호 경로 git add는 `GIT_LITERAL_PATHSPECS=1`.
- 타입 미생성 RPC/신규 테이블은 `(supabase.rpc as any)`·`(supabase.from('x' as any) as any)` 캐스팅(기존 패턴).
- 목업은 throwaway — 실 컴포넌트에 반영 후 삭제.

## 6. 최근 .ait
- 목업 최신: `019fefc1` (창고 리디자인, 상자 탭에서 봄).
